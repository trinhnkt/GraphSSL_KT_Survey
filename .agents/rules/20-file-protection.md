# File protection

Files containing `_LOCKED` are read-only unless the user explicitly approves a protocol change.

Never overwrite:
- source exports;
- prior adjudication workbooks;
- prior codebook versions.

Create a new version instead.

Before any destructive file operation, stop and request approval.
