# Systematic search

Do not run final systematic searches until the search protocol is being executed deliberately and raw exports can be archived.

Core query families:
- Q-G Graph-Based KT
- Q-S Self-Supervised KT

Supplementary:
- Q-P sparse/cold-start
- Q-R reliability/evaluation

Every search must have an exact query and result count in `search_log.csv`.
