# F3 – GRAPH‑KT TABLE III AUDIT

**Status:** TODO – Generate Table III from G_criterion.

**Required fields** (must be PASS):
- Table generated from rows where `G_criterion = Y`.
- Mandatory corrections applied (see checklist).

**Evidence sources**:
- `coding_master.csv` (or the master coding sheet containing G_criterion).

**Outputs**:
- `tables/F3_graph_papers.csv`
- `tables/F3_graph_model_entries.csv`
- Markdown summary table inserted in manuscript.

**Notes**: After generation, replace `TODO` with `PASS` and add links to the CSV files.
