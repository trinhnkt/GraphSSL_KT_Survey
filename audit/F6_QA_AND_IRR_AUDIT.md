# F6 – QA RUBRIC & IRR AUDIT

**Status:** TODO – Run frozen QA rubric (QA1‑QA8) on final coding sheet.

**Required fields** (must be PASS):
- All papers have `raw_score`, `applicable_max`, `normalized_quality`, `quality_tier` filled.
- Aggregate QA statistics recomputed.
- LIMITED tier restored if count‑zero.

**Evidence sources**:
- `coding_master.csv` (or the master coding sheet containing QA scores).

**Outputs**:
- `tables/F6_quality_assessment.csv` – detailed per‑paper QA scores.
- `tables/F6_quality_summary.csv` – aggregated statistics.

**Notes**: After audit, replace `TODO` with `PASS` and add links to the CSV files.
